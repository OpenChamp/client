All buttons have metadata "Keybind" on them, which correlates to their keybind to overwrite.

On click, they enter "listen" mode to hear your new keybind

Once heard, they check their meta keybind and attempt to assign. If it fails, pushs an error
